{
  "version": 2,
  "installCommand": "npm ci && cd backend && npm ci",
  "buildCommand": "cd backend && npm run verify",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/eventos", "destination": "/eventos/index.html" }
  ]
}
